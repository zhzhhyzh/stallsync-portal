import Spacing from "@app/constants/Spacing";

const PanelContent = {
  baseStyle: {
    ms: "auto",
    me: "auto",
    ps: `${Spacing.container}px`,
    pe: `${Spacing.container}px`,
  },
};

export const PanelContentComponent = {
  components: {
    PanelContent,
  },
};
